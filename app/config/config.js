/**
 * 系统配置
 */
//var cookie=require('../utils/cookie.js').cookie;
//var userInfo=JSON.parse(localStorage['userInfo'] || cookie('userInfo') || '{}')
module.exports={
	path:'',	//API根目录
	apiVersion:'1.0.0'	//API版本号
}